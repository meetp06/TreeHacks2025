/**
 * @typedef {'speaking' | 'listening'} AgentStatus
 */

/**
 * @typedef {'Connected' | 'Disconnected'} ConnectionStatus
 */

/**
 * @typedef {Object} ConversationConfig
 * @property {string} agentId - The ID of the agent to connect to
 * @property {Function} onConnect - Callback when connection is established
 * @property {Function} onDisconnect - Callback when connection is lost
 * @property {Function} onError - Callback when an error occurs
 * @property {Function} onModeChange - Callback when agent mode changes
 */

export const AGENT_ID = 'Wo5XTFe4QSMSRyKq5aT6';
